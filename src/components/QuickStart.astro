---
import { Code } from "astro:components";

const steps = [
  {
    step: "01",
    title: "Install",
    lang: "bash" as const,
    code: "pip install cryowire",
  },
  {
    step: "02",
    title: "Initialize a data project",
    lang: "bash" as const,
    code: `cryowire init ./my-data
cd my-data`,
  },
  {
    step: "03",
    title: "Customize components & modules",
    lang: "bash" as const,
    code: `vi components.yaml        # define your lab's parts
vi templates/*.yaml       # adjust standard wiring modules`,
  },
  {
    step: "04",
    title: "Create a new cooldown",
    lang: "bash" as const,
    code: "cryowire new your-cryo --qubits 8",
  },
  {
    step: "05",
    title: "Edit your wiring",
    lang: "yaml" as const,
    code: `# your-cryo/2026/cd001/control.yaml
modules:
  ctrl:
    stages:
      50K:
        - XMA-10dB
      4K:
        - XMA-20dB
      MXC:
        - XMA-20dB
        - Eccosorb

lines:
  - line_id: C00
    qubit: Q00
    module: ctrl`,
  },
  {
    step: "06",
    title: "Build & validate",
    lang: "bash" as const,
    code: `cryowire build your-cryo/2026/cd001/
cryowire validate your-cryo/2026/cd001/`,
  },
];
---

<section id="quickstart" class="section">
  <div class="container">
    <h2 class="section-title scroll-reveal">Quick Start</h2>
    <p class="section-subtitle scroll-reveal">
      Get up and running with cryowire in minutes.
    </p>
    <div class="steps scroll-stagger">
      {
        steps.map((item) => (
          <div class="step-card">
            <div class="step-number">{item.step}</div>
            <div class="step-body">
              <h3 class="step-title">{item.title}</h3>
              <div class="step-code-wrapper">
                <Code code={item.code} lang={item.lang} theme="github-dark" />
              </div>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>

<style>
  .steps {
    max-width: 720px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .step-card {
    display: flex;
    gap: 1.25rem;
    align-items: flex-start;
  }

  .step-number {
    flex-shrink: 0;
    width: 2.5rem;
    height: 2.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--color-primary);
    border: 1px solid rgba(56, 189, 248, 0.3);
    border-radius: 0.5rem;
    background: rgba(56, 189, 248, 0.05);
    margin-top: 0.25rem;
  }

  .step-body {
    flex: 1;
    min-width: 0;
  }

  .step-title {
    font-size: 1rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
  }

  .step-code-wrapper {
    border: 1px solid var(--color-border);
    border-radius: 0.5rem;
    overflow-x: auto;
  }

  .step-code-wrapper :global(pre) {
    margin: 0;
    padding: 1rem 1.25rem;
    border-radius: 0.5rem;
    font-size: 0.825rem;
    line-height: 1.6;
    background: var(--color-bg-card) !important;
  }

  .step-code-wrapper :global(code) {
    font-family: var(--font-mono);
  }
</style>
